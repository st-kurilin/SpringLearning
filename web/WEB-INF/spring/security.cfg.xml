<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns="http://www.springframework.org/schema/security"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://www.springframework.org/schema/security
http://www.springframework.org/schema/security/spring-security-3.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
">

    <context:component-scan base-package="com.security"/>

    <!--autowired-->
    <beans:bean id="userService" class="com.security.UserDetailsServiceImpl"/>

    <http use-expressions="true">
        <form-login
                login-page="/signin"
                default-target-url="/users"
                authentication-failure-handler-ref="authenticationBeanProcessing"
                login-processing-url="/signin_form"/>
        <logout logout-url="/signout" logout-success-url="/users"/>

        <intercept-url pattern="/resources/**" filters="none"/>
        <intercept-url pattern="/signin" access="isAnonymous()"/>
        <intercept-url pattern="/users/new" access="isAnonymous()"/>
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/products/**" access="isAuthenticated()"/>
        <intercept-url pattern="/users/*/edit" access="hasRole('ROLE_GOOD_MAN')"/>

        <remember-me key="MOVE_ME_TO_PROPERTIES"/>
        <access-denied-handler error-page="/accessDenied"/>
    </http>
    <authentication-manager>
        <authentication-provider user-service-ref="userService"/>
    </authentication-manager>
</beans:beans>